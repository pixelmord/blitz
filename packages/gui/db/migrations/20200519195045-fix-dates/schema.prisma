generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url = "***"
}

model Project {
  id           String @default(cuid()) @id
  name         String
  path         String
  description  String
  profilePhoto String
}